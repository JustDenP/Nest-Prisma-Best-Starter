export enum NodeEnv {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
}

export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum QueryOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
