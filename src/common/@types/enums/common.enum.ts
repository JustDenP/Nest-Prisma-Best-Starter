export enum NodeEnv {
  Development = 'development',
  Production = 'production',
  Test = 'test',
}

export enum Roles {
  ADMIN = 'admin',
  CLIENT = 'client',
}

export enum QueryOrder {
  ASC = 'ASC',
  DESC = 'DESC',
}
